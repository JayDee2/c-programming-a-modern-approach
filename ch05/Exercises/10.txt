The switch statment evalutes 1 % 3 (the remainder of 1 / 3) and prints the result
in string format. In this case 1 % 3 = 1, so case 1: is executed to print "one".
But due to there being no breaks in the switch, case 2: is also executed thus printing "onetwo"
